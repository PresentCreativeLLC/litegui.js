const requirejs = require("requirejs");

// Himport {require} from "require";
const Console = requirejs("./console");

// Hdocument.getElementById('consoleSection').innerHTML = new Console({prompt: "]" });